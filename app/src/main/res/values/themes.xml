<resources>
    <!-- Base application theme. -->
    <style name="Theme.RetrofitTestHilt" parent="android:Theme.Material.Light.NoActionBar">

    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/retrofit_png</item>
        <item name="postSplashScreenTheme">@style/Theme.RetrofitTestHilt</item>
    </style>
</resources>